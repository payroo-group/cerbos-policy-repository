apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "calendar"
  rules:
    - actions: ["view"]
      effect: EFFECT_ALLOW
      roles:
        - EMPLOYEE
        - MANAGER
        - ORG_ADMIN
