apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "employee-onboarding"
  rules:
    - actions: ["*"]
      effect: EFFECT_ALLOW
      roles:
        - EMPLOYEE
        - MANAGER
