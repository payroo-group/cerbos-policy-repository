apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "employee-bank-and-super-details"
  rules:
    - actions: ["verify-bank", "verify-super"]
      effect: EFFECT_ALLOW
      roles:
        - EMPLOYEE
        - MANAGER
